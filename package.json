{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@types/tape": "^4.2.32",
    "faucet": "^0.0.1",
    "lerna": "^3.8.0",
    "tape": "^4.9.1",
    "ts-node": "^7.0.1",
    "typescript": "^3.2.2"
  }
}
